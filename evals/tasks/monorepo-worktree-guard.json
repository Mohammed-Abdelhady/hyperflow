{
  "id": "monorepo-worktree-guard",
  "title": "Monorepo dirty-worktree guard and isolation helper",
  "checks": [
    "file:scripts/worktree-guard.py",
    "file:tests/test_worktree_guard.py",
    "file:docs/monorepo.md",
    "run:python3 scripts/worktree-guard.py --help",
    "run:python3 -m unittest tests.test_worktree_guard -v",
    "run:python3 -c \"import pathlib; t=pathlib.Path('docs/monorepo.md').read_text(); assert 'safe_for_in_place' in t and 'git worktree add' in t\""
  ]
}