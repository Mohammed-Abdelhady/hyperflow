# T7 — Doctrine + references

**Specialist:** `docs`, `architect` · **Batch 4** · depends T6

## Task
Update the doctrine and reference docs so reap-on-completion is the documented terminal step of the task lifecycle, with the disposition policy and the memory-preservation guarantee stated once, authoritatively.

## Why
Layer 10 currently tells the closing skill to run `archive-artefacts.py --file` — that's now superseded by the reap phase. The lifecycle docs must describe the real behavior or they drift (the repo ships a drift detector that will flag it).

## Scope
- IN: doctrine + reference prose only.
- OUT: no code; no new behavior.

## Files in scope
- `skills/hyperflow/DOCTRINE.md` — **Layer 10 (Hygiene, `:597-608`)**: rewrite from "closing skill runs `archive-artefacts.py --file`" to "closing skill runs the **reap phase** for the finished slug (archive-first, memory-preserving, reports)." Reference `cleanup.reapOnComplete`.
- `skills/hyperflow/task-tracking.md` — the status lifecycle (`:91-114`, esp. `Review → APPROVED → DELETE task file` at `:112`) becomes `Review → APPROVED → reap (archive scope + optimize memory + report)`. Note brief dirs are reaped with the task.
- `skills/hyperflow/feature-phases.md` — feature completion (`:138,149-150`) points to reap for the whole tree.
- `skills/hyperflow/memory-system.md` — add the explicit guarantee: reap never deletes durable entries; it only rebuilds `index.md`, drops orphaned refs, and compacts (ties to `:167-201`).

## Acceptance criteria
1. Layer 10 describes reap-on-completion, not the bare `--file` call.
2. Task + feature lifecycle docs name reap as the terminal step.
3. memory-system states the preservation guarantee.
4. The reference-drift detector reports no new drift.

## Test cases
- Run the drift detector (from feature `hyperflow-next` phase-5) → clean.
- Grep DOCTRINE/task-tracking/feature-phases/memory-system for stale "DELETE task file" / "--file" archival language → none left where reap now applies.
- **E2E:** a reader following task-tracking's lifecycle arrives at reap as the documented end.

## Related context
- Layer 10: `skills/hyperflow/DOCTRINE.md:597-608`.
- Lifecycle: `skills/hyperflow/task-tracking.md:91-114` (`:112` delete, `:160` "delete only when done").
- Feature archival: `skills/hyperflow/feature-phases.md:138,149-150`.
- Memory prune/preserve: `skills/hyperflow/memory-system.md:167-201`.
- Drift detector: shipped in commit `5740bdf` (reference-drift detector).

## Gotchas
- The daily session-start sweep still exists as the safety net — describe reap as the *primary/explicit* terminal cleanup, the sweep as the *fallback* for unreaped stale artefacts. Don't imply the sweep is removed.
- Respect each doc's per-file line budget (task-tracking/feature-phases were recently split to stay under limits).
