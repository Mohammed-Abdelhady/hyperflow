# Worker Prompt Template (canonical reference)

The complete worker contract lives at [`skills/hyperflow/worker-prompt.md`](../../hyperflow/worker-prompt.md). Load that canonical file when rendering a worker brief; do not maintain a second copy here.

This reference remains under the dispatch skill so portable hosts can resolve the existing link. The canonical contract, including host-operation mapping, role separation, evidence requirements, acceptance criteria, and output format, is authoritative.
