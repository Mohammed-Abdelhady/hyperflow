node_modules/
.DS_Store
*.log
.env
.env.*
.in_use/
.hyperflow/
# NOTE: .hyperflow-handoff/ is intentionally NOT ignored — the two-session handoff
# package must travel via git so a build session in another environment can read it.
.claude/
.test/

__pycache__/
*.py[cod]
