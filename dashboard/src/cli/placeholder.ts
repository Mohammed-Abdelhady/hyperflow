/**
 * Temporary CLI placeholder — replaced by real entry modules in phase 4.
 * Keeps the cli project-reference graph green until then.
 */
export function placeholderCliVersion(): string {
  return "0.0.0-scaffold";
}
