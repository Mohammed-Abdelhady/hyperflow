/**
 * Temporary shared placeholder — replaced by Zod schema modules in T2.
 * Dependency arrows point inward only: shared imports neither server nor client.
 */
export const PLACEHOLDER_SHARED = "shared-scaffold" as const;
